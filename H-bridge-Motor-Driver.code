// C++ code
#define a1 2 
#define a2 3 
#define a3 4 
#define a4 5 

void setup()
{
   pinMode(a1,OUTPUT);
   pinMode(a2,OUTPUT);
   pinMode(a3,OUTPUT);
   pinMode(a4,OUTPUT);
}

void loop()
{
  digitalWrite(a1, HIGH);
  digitalWrite(a2, LOW);
  digitalWrite(a3, HIGH);
  digitalWrite(a4, LOW);
  delay (1000);
}
